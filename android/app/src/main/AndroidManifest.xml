<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- crmex.md §10.1 — corrected manifest. Every permission here maps to a
         concrete need; nothing is added speculatively. -->

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- Not used by the app. @capacitor-community/contacts declares READ and
         WRITE under one "contacts" alias, and Capacitor rejects the permission
         request unless every permission in the alias is in the manifest. -->
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Required from API 34 (Android 14) alongside foregroundServiceType="dataSync" -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Required from API 33 (Android 13) to show the foreground service notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Only needed on API <=28 for MediaStore-less external writes; scoped
         storage on API 29+ (our test device) does not need this for the
         MediaStore insert path used by saveImageToLibrary (crmex.md §8.4). -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Play Store: this is a restricted permission requiring a policy
         exemption. Sideload/internal builds only — see android/README.md
         "Distribution" section. Left declared here since the current
         distribution path is undecided (crmex.md §10.3, §14); strip this
         permission from the manifest before any Play submission unless an
         exemption has been granted. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Firm invitation links (crmex://invite/<token>) from an email or
                 a scanned QR code open the app's join screen (crmex.md §15.2).
                 The token is only a claim; core-server validates it on accept. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="crmex" android:host="invite" />
            </intent-filter>

        </activity>

        <!-- Invitation QR codes are scanned with Google's code scanner (Play
             services UI), which needs no camera permission in this app. -->
        <meta-data android:name="com.google.mlkit.vision.DEPENDENCIES" android:value="barcode_ui" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Started when a send batch begins, stopped when the queue drains
             (crmex.md §10.1, §10.2; build order step 8). Hosts no logic
             itself beyond lifecycle + notification — Baileys runs in the
             embedded Node process via capacitor-nodejs, not inside this
             service. Its job is purely to keep the process alive and show
             the user why (AND-01, AND-02, AND-06). -->
        <service
            android:name="com.crmex.gateway.BackgroundEngineService"
            android:foregroundServiceType="dataSync"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>
